#pragma once

namespace Stage
{
	namespace GravityBoxParam
	{
		constexpr float SCALE = 0.1f;
		constexpr float WIDTH = 768.f;
		constexpr float HEIGHT = 768.f;

		constexpr float COLLIDER2_DELTA_LENGTH = 2.f;

		constexpr float CAMERA_DELTA_ROT = 0.1f;

		namespace AppleParam
		{
			constexpr float MOVE_LENGTH = 15.f;
			constexpr float MOVE_SPEED = 3.f;
			constexpr float ROTATION_SPEED = 0.7f;

		}
	}
}